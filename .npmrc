# Prevent platform-specific dependencies
target_platform=linux
target_arch=x64
target_libc=glibc

# Allow optional dependencies (needed for esbuild platform binaries)
optional=true
include=optional

# Ignore scripts during install
ignore-scripts=true

# Skip audit and funding
audit=false
fund=false